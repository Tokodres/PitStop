<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PitStop">
        <activity
            android:name=".ui.login.PantallaListado"
            android:exported="false"
            android:label="@string/title_activity_pantalla_listado" />
        <activity
            android:name=".ui.login.PantallaResumen"
            android:exported="false"
            android:label="@string/title_activity_pantalla_resumen" />
        <activity
            android:name=".Vista.PantallaRegistro"
            android:exported="false" />
        <activity
            android:name=".Vista.PantallaInicio"
            android:exported="false" />
        <activity
            android:name=".Vista.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PitStop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>